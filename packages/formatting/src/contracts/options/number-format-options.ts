/**
 * Options for number presentation formatting.
 */
export interface NumberFormatOptions {
  readonly useGrouping?: boolean;
  readonly minimumFractionDigits?: number;
  readonly maximumFractionDigits?: number;
}
