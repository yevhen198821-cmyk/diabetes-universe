openapi: 3.0.3
info:
  title: Diabetes Universe Medical API
  version: v1
  description: |
    Self-subject medical event transport API for Diabetes Universe.
    Public contracts expose opaque resource identifiers and revision tokens only.
servers:
  - url: /api/v1/medical
    description: Versioned medical API base path
tags:
  - name: MedicalEvents
    description: Self-subject medical event resources
  - name: MedicalAdoption
    description: One-time local data adoption sessions
  - name: DiabetesSettings
    description: Self-subject diabetes settings and glucose target profile
security:
  - sessionAuth: []
paths:
  /me/medical-events:
    get:
      tags: [MedicalEvents]
      summary: List medical events
      operationId: listMedicalEvents
      parameters:
        - $ref: '#/components/parameters/LimitQuery'
        - $ref: '#/components/parameters/CursorQuery'
      responses:
        '200':
          description: Paginated medical event collection
          headers:
            Cache-Control:
              $ref: '#/components/headers/PrivateNoStore'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MedicalEventListResponse'
        '400':
          $ref: '#/components/responses/InvalidCursor'
        '401':
          $ref: '#/components/responses/AuthRequired'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
    post:
      tags: [MedicalEvents]
      summary: Create medical event
      operationId: createMedicalEvent
      parameters:
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MedicalEventCreateRequest'
      responses:
        '201':
          description: Created medical event
          headers:
            Cache-Control:
              $ref: '#/components/headers/PrivateNoStore'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MedicalEventResource'
        '401':
          $ref: '#/components/responses/AuthRequired'
        '409':
          $ref: '#/components/responses/IdempotencyConflict'
        '413':
          $ref: '#/components/responses/RequestTooLarge'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /me/medical-events/{resourceId}:
    parameters:
      - $ref: '#/components/parameters/ResourceIdPath'
    get:
      tags: [MedicalEvents]
      summary: Read one medical event
      operationId: getMedicalEvent
      responses:
        '200':
          description: Medical event resource
          headers:
            Cache-Control:
              $ref: '#/components/headers/PrivateNoStore'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MedicalEventResource'
        '401':
          $ref: '#/components/responses/AuthRequired'
        '404':
          $ref: '#/components/responses/ResourceNotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
    patch:
      tags: [MedicalEvents]
      summary: Update medical event
      operationId: updateMedicalEvent
      parameters:
        - $ref: '#/components/parameters/IfMatchHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MedicalEventUpdateRequest'
      responses:
        '200':
          description: Updated medical event
          headers:
            Cache-Control:
              $ref: '#/components/headers/PrivateNoStore'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MedicalEventResource'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/AuthRequired'
        '404':
          $ref: '#/components/responses/ResourceNotFound'
        '412':
          $ref: '#/components/responses/RevisionConflict'
        '413':
          $ref: '#/components/responses/RequestTooLarge'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
    delete:
      tags: [MedicalEvents]
      summary: Delete medical event
      operationId: deleteMedicalEvent
      parameters:
        - $ref: '#/components/parameters/IfMatchHeader'
      responses:
        '204':
          description: Resource deleted
          headers:
            Cache-Control:
              $ref: '#/components/headers/PrivateNoStore'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/AuthRequired'
        '404':
          $ref: '#/components/responses/ResourceNotFound'
        '412':
          $ref: '#/components/responses/RevisionConflict'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /me/adoption-sessions:
    post:
      tags: [MedicalAdoption]
      summary: Create or resume adoption session
      operationId: createAdoptionSession
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdoptionSessionCreateRequest'
      responses:
        '200':
          description: Adoption session state
          headers:
            Cache-Control:
              $ref: '#/components/headers/PrivateNoStore'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdoptionSessionResponse'
        '401':
          $ref: '#/components/responses/AuthRequired'
        '403':
          $ref: '#/components/responses/ValidationFailed'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/RateLimited'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /me/adoption-sessions/{adoptionSessionId}:
    get:
      tags: [MedicalAdoption]
      summary: Get adoption session status
      operationId: getAdoptionSession
      parameters:
        - $ref: '#/components/parameters/AdoptionSessionId'
      responses:
        '200':
          description: Adoption session state
          headers:
            Cache-Control:
              $ref: '#/components/headers/PrivateNoStore'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdoptionSessionResponse'
        '401':
          $ref: '#/components/responses/AuthRequired'
        '404':
          $ref: '#/components/responses/ResourceNotFound'
        '429':
          $ref: '#/components/responses/RateLimited'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /me/adoption-sessions/{adoptionSessionId}/items:
    post:
      tags: [MedicalAdoption]
      summary: Submit bounded adoption batch
      operationId: adoptBatch
      parameters:
        - $ref: '#/components/parameters/AdoptionSessionId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdoptionBatchRequest'
      responses:
        '200':
          description: Per-item adoption outcomes
          headers:
            Cache-Control:
              $ref: '#/components/headers/PrivateNoStore'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdoptionBatchResponse'
        '401':
          $ref: '#/components/responses/AuthRequired'
        '404':
          $ref: '#/components/responses/ResourceNotFound'
        '409':
          $ref: '#/components/responses/ValidationFailed'
        '413':
          $ref: '#/components/responses/RequestTooLarge'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/RateLimited'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /me/adoption-sessions/{adoptionSessionId}/complete:
    post:
      tags: [MedicalAdoption]
      summary: Complete adoption session
      operationId: completeAdoptionSession
      parameters:
        - $ref: '#/components/parameters/AdoptionSessionId'
      responses:
        '200':
          description: Completed adoption session
          headers:
            Cache-Control:
              $ref: '#/components/headers/PrivateNoStore'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdoptionSessionResponse'
        '401':
          $ref: '#/components/responses/AuthRequired'
        '404':
          $ref: '#/components/responses/ResourceNotFound'
        '409':
          $ref: '#/components/responses/ValidationFailed'
        '429':
          $ref: '#/components/responses/RateLimited'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /me/adoption-sessions/{adoptionSessionId}/cancel:
    post:
      tags: [MedicalAdoption]
      summary: Cancel adoption session
      operationId: cancelAdoptionSession
      parameters:
        - $ref: '#/components/parameters/AdoptionSessionId'
      responses:
        '200':
          description: Cancelled adoption session
          headers:
            Cache-Control:
              $ref: '#/components/headers/PrivateNoStore'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdoptionSessionResponse'
        '401':
          $ref: '#/components/responses/AuthRequired'
        '404':
          $ref: '#/components/responses/ResourceNotFound'
        '429':
          $ref: '#/components/responses/RateLimited'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /me/diabetes-settings:
    get:
      tags: [DiabetesSettings]
      summary: Read diabetes settings
      operationId: getDiabetesSettings
      responses:
        '200':
          description: Current diabetes settings for the authenticated self subject
          headers:
            Cache-Control:
              $ref: '#/components/headers/PrivateNoStore'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DiabetesSettingsResponse'
        '401':
          $ref: '#/components/responses/AuthRequired'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
    patch:
      tags: [DiabetesSettings]
      summary: Update diabetes settings
      operationId: patchDiabetesSettings
      parameters:
        - $ref: '#/components/parameters/IfMatchHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DiabetesSettingsPatchRequest'
      responses:
        '200':
          description: Updated diabetes settings
          headers:
            Cache-Control:
              $ref: '#/components/headers/PrivateNoStore'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DiabetesSettingsResponse'
        '401':
          $ref: '#/components/responses/AuthRequired'
        '412':
          $ref: '#/components/responses/RevisionConflict'
        '413':
          $ref: '#/components/responses/RequestTooLarge'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /me/glucose-target-profile:
    get:
      tags: [DiabetesSettings]
      summary: Read glucose target profile
      operationId: getGlucoseTargetProfile
      responses:
        '200':
          description: Current glucose target profile for the authenticated self subject
          headers:
            Cache-Control:
              $ref: '#/components/headers/PrivateNoStore'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GlucoseTargetProfileResponse'
        '401':
          $ref: '#/components/responses/AuthRequired'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
    put:
      tags: [DiabetesSettings]
      summary: Set glucose target profile default range
      operationId: putGlucoseTargetProfile
      parameters:
        - $ref: '#/components/parameters/IfMatchHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GlucoseTargetProfilePutRequest'
      responses:
        '200':
          description: Updated glucose target profile
          headers:
            Cache-Control:
              $ref: '#/components/headers/PrivateNoStore'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GlucoseTargetProfileResponse'
        '401':
          $ref: '#/components/responses/AuthRequired'
        '412':
          $ref: '#/components/responses/RevisionConflict'
        '413':
          $ref: '#/components/responses/RequestTooLarge'
        '422':
          $ref: '#/components/responses/ValidationFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
    delete:
      tags: [DiabetesSettings]
      summary: Clear glucose target profile default range
      operationId: deleteGlucoseTargetProfile
      parameters:
        - $ref: '#/components/parameters/IfMatchHeader'
      responses:
        '200':
          description: Cleared glucose target profile
          headers:
            Cache-Control:
              $ref: '#/components/headers/PrivateNoStore'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GlucoseTargetProfileResponse'
        '401':
          $ref: '#/components/responses/AuthRequired'
        '412':
          $ref: '#/components/responses/RevisionConflict'
        '428':
          $ref: '#/components/responses/PreconditionRequired'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
components:
  securitySchemes:
    sessionAuth:
      type: apiKey
      in: cookie
      name: session
      description: Validated Better Auth session cookie resolved server-side
  parameters:
    ResourceIdPath:
      name: resourceId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    LimitQuery:
      name: limit
      in: query
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 100
    CursorQuery:
      name: cursor
      in: query
      required: false
      schema:
        type: string
        maxLength: 4096
    IdempotencyKeyHeader:
      name: Idempotency-Key
      in: header
      required: true
      schema:
        type: string
        maxLength: 128
        pattern: '^[A-Za-z0-9._-]+$'
    IfMatchHeader:
      name: If-Match
      in: header
      required: true
      schema:
        type: string
        description: Opaque revision token previously returned as revision or ETag
    AdoptionSessionId:
      name: adoptionSessionId
      in: path
      required: true
      schema:
        type: string
        format: uuid
  headers:
    PrivateNoStore:
      description: Private response that must not be cached publicly
      schema:
        type: string
        example: private, no-store
    ETag:
      description: Opaque revision token for optimistic concurrency
      schema:
        type: string
    RetryAfter:
      description: Seconds until a rate-limited client may retry
      schema:
        type: integer
        minimum: 1
  schemas:
    MedicalEventCreateRequest:
      type: object
      required: [event]
      additionalProperties: false
      properties:
        event:
          $ref: '#/components/schemas/SemanticTimelineEvent'
    MedicalEventUpdateRequest:
      type: object
      required: [event]
      additionalProperties: false
      properties:
        event:
          $ref: '#/components/schemas/SemanticTimelineEvent'
    MedicalEventResource:
      type: object
      required:
        - resourceId
        - revision
        - createdAt
        - updatedAt
        - deletedAt
        - event
      properties:
        resourceId:
          type: string
          format: uuid
        revision:
          type: string
          description: Opaque revision token
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        deletedAt:
          type: string
          format: date-time
          nullable: true
        event:
          $ref: '#/components/schemas/SemanticTimelineEvent'
    MedicalEventListResponse:
      type: object
      required: [items, page]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/MedicalEventResource'
        page:
          type: object
          required: [nextCursor, hasMore]
          properties:
            nextCursor:
              type: string
              nullable: true
            hasMore:
              type: boolean
    AdoptionSessionCreateRequest:
      type: object
      required:
        - clientAdoptionRunId
        - sourcePlatform
        - sourceAppVersion
        - sourceSchemaMin
        - sourceSchemaMax
      additionalProperties: false
      properties:
        clientAdoptionRunId:
          type: string
          maxLength: 128
          pattern: '^[A-Za-z0-9._-]+$'
        sourcePlatform:
          type: string
          maxLength: 16
        sourceAppVersion:
          type: string
          maxLength: 64
        sourceSchemaMin:
          type: integer
          minimum: 1
        sourceSchemaMax:
          type: integer
          minimum: 1
        eligibleCount:
          type: integer
          minimum: 0
    AdoptionSession:
      type: object
      required:
        - adoptionSessionId
        - clientAdoptionRunId
        - sourcePlatform
        - sourceAppVersion
        - sourceSchemaMin
        - sourceSchemaMax
        - lifecycleState
        - eligibleCount
        - adoptedCount
        - skippedCount
        - failedCount
        - createdAt
        - startedAt
        - completedAt
        - updatedAt
      properties:
        adoptionSessionId:
          type: string
          format: uuid
        clientAdoptionRunId:
          type: string
        sourcePlatform:
          type: string
        sourceAppVersion:
          type: string
        sourceSchemaMin:
          type: integer
        sourceSchemaMax:
          type: integer
        lifecycleState:
          type: string
          enum: [open, failed, completed, cancelled]
        eligibleCount:
          type: integer
        adoptedCount:
          type: integer
        skippedCount:
          type: integer
        failedCount:
          type: integer
        createdAt:
          type: string
          format: date-time
        startedAt:
          type: string
          format: date-time
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true
        updatedAt:
          type: string
          format: date-time
    AdoptionSessionResponse:
      type: object
      required: [session]
      properties:
        session:
          $ref: '#/components/schemas/AdoptionSession'
    AdoptionBatchRequest:
      type: object
      required: [items]
      additionalProperties: false
      properties:
        items:
          type: array
          minItems: 1
          maxItems: 100
          items:
            $ref: '#/components/schemas/AdoptionItemInput'
    AdoptionItemInput:
      type: object
      required:
        - sourceNamespace
        - localEventId
        - sourceSchemaVersion
        - event
      additionalProperties: false
      properties:
        sourceNamespace:
          type: string
          maxLength: 128
          pattern: '^[A-Za-z0-9._-]+$'
        localEventId:
          type: string
          maxLength: 128
          pattern: '^[A-Za-z0-9._-]+$'
        sourceSchemaVersion:
          type: integer
          minimum: 1
        event:
          $ref: '#/components/schemas/SemanticTimelineEvent'
    AdoptionBatchResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            oneOf:
              - $ref: '#/components/schemas/AdoptionItemSuccess'
              - $ref: '#/components/schemas/AdoptionItemReplay'
              - $ref: '#/components/schemas/AdoptionItemFailure'
    AdoptionItemSuccess:
      type: object
      required: [localEventId, status, resourceId, revision, createdAt]
      properties:
        localEventId:
          type: string
        status:
          type: string
          enum: [adopted]
        resourceId:
          type: string
          format: uuid
        revision:
          type: string
        createdAt:
          type: string
          format: date-time
    AdoptionItemReplay:
      type: object
      required: [localEventId, status, resourceId, revision]
      properties:
        localEventId:
          type: string
        status:
          type: string
          enum: [already_adopted]
        resourceId:
          type: string
          format: uuid
        revision:
          type: string
    AdoptionItemFailure:
      type: object
      required: [localEventId, status, code, message]
      properties:
        localEventId:
          type: string
        status:
          type: string
          enum: [failed]
        code:
          type: string
        message:
          type: string
    GlucoseDisplayUnit:
      type: string
      enum: [mmol_per_l, mg_per_dl]
      description: Subject-scoped glucose display preference (domain code)
    DiabetesTypeCategory:
      type: string
      enum: [type_1, type_2, gestational, other, unknown]
      description: Informational self-reported diabetes type category
    DiabetesTypeClassification:
      type: object
      required: [category, source]
      additionalProperties: false
      properties:
        category:
          $ref: '#/components/schemas/DiabetesTypeCategory'
        otherDescriptor:
          type: string
          nullable: true
          maxLength: 256
        source:
          type: string
          enum: [self_reported]
    TargetRangeSource:
      type: string
      enum: [user_defined, clinician_defined, imported, system_reference]
      description: Provenance for glucose target values
    GlucoseTargetRange:
      type: object
      required: [lowMmolPerL, highMmolPerL, source]
      additionalProperties: false
      properties:
        lowMmolPerL:
          type: number
          minimum: 0.1
          maximum: 100
        highMmolPerL:
          type: number
          minimum: 0.1
          maximum: 100
        source:
          $ref: '#/components/schemas/TargetRangeSource'
    DiabetesSettings:
      type: object
      description: Persisted diabetes settings resource (internal schema)
      required:
        - settingsId
        - subjectId
        - glucoseDisplayUnit
        - diabetesType
        - createdAt
        - updatedAt
        - revision
      additionalProperties: false
      properties:
        settingsId:
          type: string
          format: uuid
        subjectId:
          type: string
          format: uuid
        glucoseDisplayUnit:
          allOf:
            - $ref: '#/components/schemas/GlucoseDisplayUnit'
          nullable: true
          description: Null indicates transitional unset state before explicit user choice
        diabetesType:
          $ref: '#/components/schemas/DiabetesTypeClassification'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        revision:
          type: string
          description: Opaque revision token for optimistic concurrency
    DiabetesSettingsResponse:
      type: object
      required:
        - configured
        - subjectId
        - glucoseDisplayUnit
        - diabetesType
        - revision
      additionalProperties: false
      properties:
        configured:
          type: boolean
          description: False when no persisted settings row exists yet
        settingsId:
          type: string
          format: uuid
          nullable: true
        subjectId:
          type: string
          format: uuid
        glucoseDisplayUnit:
          allOf:
            - $ref: '#/components/schemas/GlucoseDisplayUnit'
          nullable: true
        diabetesType:
          $ref: '#/components/schemas/DiabetesTypeClassification'
        createdAt:
          type: string
          format: date-time
          nullable: true
        updatedAt:
          type: string
          format: date-time
          nullable: true
        revision:
          type: string
          description: Opaque revision token for optimistic concurrency
    DiabetesSettingsPatchRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        glucoseDisplayUnit:
          allOf:
            - $ref: '#/components/schemas/GlucoseDisplayUnit'
          nullable: true
        diabetesType:
          $ref: '#/components/schemas/DiabetesTypeClassification'
    GlucoseTargetProfile:
      type: object
      description: Persisted glucose target profile resource (internal schema)
      required:
        - profileId
        - subjectId
        - defaultRange
        - createdAt
        - updatedAt
        - revision
      additionalProperties: false
      properties:
        profileId:
          type: string
          format: uuid
        subjectId:
          type: string
          format: uuid
        defaultRange:
          allOf:
            - $ref: '#/components/schemas/GlucoseTargetRange'
          nullable: true
          description: Null when no target range is configured
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        revision:
          type: string
          description: Opaque revision token for optimistic concurrency
    GlucoseTargetProfileResponse:
      type: object
      required:
        - configured
        - subjectId
        - defaultRange
        - revision
      additionalProperties: false
      properties:
        configured:
          type: boolean
          description: True when a personalized defaultRange is configured
        profileId:
          type: string
          format: uuid
          nullable: true
        subjectId:
          type: string
          format: uuid
        defaultRange:
          allOf:
            - $ref: '#/components/schemas/GlucoseTargetRange'
          nullable: true
        createdAt:
          type: string
          format: date-time
          nullable: true
        updatedAt:
          type: string
          format: date-time
          nullable: true
        revision:
          type: string
          description: Opaque revision token for optimistic concurrency
    GlucoseTargetProfilePutRequest:
      type: object
      required: [defaultRange]
      additionalProperties: false
      properties:
        defaultRange:
          type: object
          required: [lowMmolPerL, highMmolPerL]
          additionalProperties: false
          properties:
            lowMmolPerL:
              type: number
              minimum: 0.1
              maximum: 100
            highMmolPerL:
              type: number
              minimum: 0.1
              maximum: 100
            source:
              type: string
              enum: [user_defined]
              description: Optional; server forces user_defined for self-service writes
    SemanticTimelineEvent:
      type: object
      required: [occurredAt, schemaVersion, source, kind]
      description: Transport-safe semantic event payload without server-owned lifecycle fields
      properties:
        occurredAt:
          type: string
          format: date-time
        schemaVersion:
          type: integer
          enum: [1]
        source:
          type: string
          enum: [manual, device, import]
        kind:
          type: string
          enum: [glucose, insulin, nutrition, medication, activity, note]
    ApiErrorEnvelope:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message, correlationId, details]
          properties:
            code:
              type: string
              enum:
                - AUTH_REQUIRED
                - AUTH_INSUFFICIENT
                - SUBJECT_ACCESS_DENIED
                - RESOURCE_NOT_FOUND
                - VALIDATION_FAILED
                - INVALID_CURSOR
                - PRECONDITION_REQUIRED
                - REVISION_CONFLICT
                - IDEMPOTENCY_CONFLICT
                - RATE_LIMITED
                - REQUEST_TOO_LARGE
                - SERVICE_UNAVAILABLE
                - INTERNAL_ERROR
            message:
              type: string
            correlationId:
              type: string
              format: uuid
            details:
              type: object
              nullable: true
              additionalProperties: true
  responses:
    AuthRequired:
      description: Authentication is required
      headers:
        Cache-Control:
          $ref: '#/components/headers/PrivateNoStore'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorEnvelope'
          example:
            error:
              code: AUTH_REQUIRED
              message: Authentication is required.
              correlationId: 00000000-0000-4000-8000-000000000001
              details: null
    ResourceNotFound:
      description: Resource unavailable or out of scope
      headers:
        Cache-Control:
          $ref: '#/components/headers/PrivateNoStore'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorEnvelope'
    ValidationFailed:
      description: Request validation failed
      headers:
        Cache-Control:
          $ref: '#/components/headers/PrivateNoStore'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorEnvelope'
    InvalidCursor:
      description: Pagination cursor is invalid
      headers:
        Cache-Control:
          $ref: '#/components/headers/PrivateNoStore'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorEnvelope'
    PreconditionRequired:
      description: If-Match header is required
      headers:
        Cache-Control:
          $ref: '#/components/headers/PrivateNoStore'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorEnvelope'
    RevisionConflict:
      description: Stale revision token
      headers:
        Cache-Control:
          $ref: '#/components/headers/PrivateNoStore'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorEnvelope'
    IdempotencyConflict:
      description: Idempotency key reused with different payload
      headers:
        Cache-Control:
          $ref: '#/components/headers/PrivateNoStore'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorEnvelope'
    RequestTooLarge:
      description: Request body exceeds transport limit
      headers:
        Cache-Control:
          $ref: '#/components/headers/PrivateNoStore'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorEnvelope'
    RateLimited:
      description: Rate limit exceeded
      headers:
        Cache-Control:
          $ref: '#/components/headers/PrivateNoStore'
        Retry-After:
          $ref: '#/components/headers/RetryAfter'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorEnvelope'
    ServiceUnavailable:
      description: Temporary service unavailability
      headers:
        Cache-Control:
          $ref: '#/components/headers/PrivateNoStore'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorEnvelope'
    InternalError:
      description: Unexpected server error
      headers:
        Cache-Control:
          $ref: '#/components/headers/PrivateNoStore'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorEnvelope'
